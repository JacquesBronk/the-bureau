# The Bureau — MCP Server Documentation

Architecture and reference documentation for The Bureau's MCP server (the orchestration engine).

Start with **[Overview](Overview.md)** — the map of the whole system; every note below is reachable from it.

## Sections

- **[Architecture](Architecture/)** — component map and the life of a task graph
- **[Subsystems](Subsystems/)** — the engine's internal parts (task graph, spawn, state machine, telemetry, …)
- **[Reference](Reference/)** — tool catalog, agent catalog, shared types
- **[Operations](Operations/)** — build, test, and runbook guides

---

*These pages are generated from The Bureau's internal documentation vault and converted to plain
GitHub Markdown. Code citations (e.g. `src/mcp-server.ts:172`) point at this repository's source.*
