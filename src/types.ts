// Backward-compatibility barrel — all types have moved to src/types/
// Existing imports of `from "./types.js"` or `from "../types.js"` continue to work unchanged.

// Re-export everything from the new organised type modules
export * from "./types/index.js";
