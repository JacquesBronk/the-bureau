# integration-ref-proof

This fixture proves exec pod clones the integration ref (merged candidate), not base. Used in live validation tier (2i). Unit test only asserts the gitBaseRef parameter is set correctly.
